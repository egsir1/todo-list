import React from "react";
import classes from "./Header.module.css";

const Header = () => {
  return (
    <div>
      <h1 className={classes.text}>To-do List</h1>
    </div>
  );
};

export default Header;
